<UserControl x:Class="milestone1.DataList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:milestone1"
             mc:Ignorable="d" 
             d:DesignHeight="292" d:DesignWidth="242">
    <Grid d:DataContext="{d:DesignInstance Type=local:FlightGearViewModel}">
        <ListBox
            x:Name="dataListBox"
            HorizontalContentAlignment="Center"
            SelectionChanged="ListBox_SelectionChanged" ItemsSource="{Binding VM_Properties}"  
            
            >
        </ListBox>

    </Grid>
</UserControl>
